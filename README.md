# mirdaily_app
la carpeta local viva sigue siendo mirdailyapp/Version final v4, y a partir de ahora los cambios ahí deberían acabar en commits, igual que backend y frontend. 
A diferencia de esos dos, este repo no tiene deploy automático — no hay tienda conectada, así que instalar sigue siendo flutter build/flutter install a mano.
